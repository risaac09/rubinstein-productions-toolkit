[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "context-provenance"
version = "0.1.0"
description = "Four-channel attribution for AI-mediated text"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "Isaac Rubinstein", email = "risaac09@gmail.com"}]

[project.scripts]
provenance = "provenance.cli:main"
